{
  agents: {
    defaults: {
      heartbeat: {
        every: "30m",
        target: "openclaw-weixin",
        lightContext: false,
        activeHours: { start: "08:00", end: "22:00", timezone: "Asia/Tokyo" }
      }
    }
  }
}
